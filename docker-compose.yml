version: "3.8"
services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: ems-frontend:latest
    ports:
      - "5173:80" # map host 5173 to container nginx 80
    environment:
      - NODE_ENV=production
    restart: unless-stopped
